{
  "dashboard": {
    "id": null,
    "uid": null,
    "title": "Prophet - Model Monitoring",
    "panels": [
      {
        "type": "graph",
        "title": "Request Rate",
        "targets": [ { "expr": "app_requests_total", "format": "time_series" } ]
      },
      {
        "type": "graph",
        "title": "Request Latency (s)",
        "targets": [ { "expr": "histogram_quantile(0.95, sum(rate(app_request_latency_seconds_bucket[5m])) by (le))", "format": "time_series" } ]
      }
    ]
  }
}